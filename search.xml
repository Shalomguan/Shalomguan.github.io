<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Mongoose 里「变量名、模型名、集合名」到底有什么区别？</title>
      <link href="/2025/12/05/Mongoose%20%E9%87%8C%E5%8F%98%E9%87%8F%E5%90%8D%E3%80%81%E6%A8%A1%E5%9E%8B%E5%90%8D%E3%80%81%E9%9B%86%E5%90%88%E5%90%8D%E7%9A%84%E5%8C%BA%E5%88%AB/"/>
      <url>/2025/12/05/Mongoose%20%E9%87%8C%E5%8F%98%E9%87%8F%E5%90%8D%E3%80%81%E6%A8%A1%E5%9E%8B%E5%90%8D%E3%80%81%E9%9B%86%E5%90%88%E5%90%8D%E7%9A%84%E5%8C%BA%E5%88%AB/</url>
      
        <content type="html"><![CDATA[<p>最近在学习fullstackopen的时候被mogos的变量名，模型名和集合名的概念搞得晕晕的</p><p>踩坑几次之后，索性写了一次博客：<strong>在 Mongoose 里，下面这三个名字分别是什么、干嘛用、有什么关系。</strong></p><span id="more"></span>><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title class_">Person</span> = mongoose.<span class="title function_">model</span>(<span class="string">&#x27;Person&#x27;</span>, phonebookSchema)</span><br><span class="line"><span class="string">``</span><span class="string">``</span></span><br><span class="line"></span><br><span class="line">这一行里，至少出现了 **三个名字**：</span><br><span class="line"></span><br><span class="line">* <span class="variable constant_">JS</span> 变量名：<span class="string">`Person`</span></span><br><span class="line">* 模型名（<span class="title class_">Model</span> name）：<span class="string">`&#x27;Person&#x27;`</span></span><br><span class="line">* 集合名（<span class="title class_">Collection</span> name，很多人会叫“表名”）</span><br><span class="line">&lt;!-- more --&gt;</span><br><span class="line">## <span class="number">1.</span> 变量名：<span class="title class_">Person</span> —— 只是你代码里的一个变量</span><br><span class="line"></span><br><span class="line">先看左边：</span><br><span class="line"></span><br><span class="line"><span class="string">``</span><span class="string">`js</span></span><br><span class="line"><span class="string">const Person = ...</span></span><br><span class="line"><span class="string">//    ^^^^^^  只是普通的 JS 常量名字</span></span><br></pre></td></tr></table></figure><p>这个 <code>Person</code> 只是一个普通的 js 变量名，你可以叫：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> P = mongoose.<span class="title function_">model</span>(<span class="string">&#x27;Person&#x27;</span>, phonebookSchema)</span><br><span class="line"><span class="keyword">const</span> <span class="title class_">MyAwesomeModel</span> = mongoose.<span class="title function_">model</span>(<span class="string">&#x27;Person&#x27;</span>, phonebookSchema)</span><br></pre></td></tr></table></figure><p>只要后面代码统一用这个变量就行，比如：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> person = <span class="keyword">new</span> <span class="title class_">Person</span>(&#123; <span class="attr">name</span>: <span class="string">&#x27;Alice&#x27;</span>, <span class="attr">number</span>: <span class="string">&#x27;12345&#x27;</span> &#125;)</span><br><span class="line">person.<span class="title function_">save</span>()</span><br><span class="line"></span><br><span class="line"><span class="title class_">Person</span>.<span class="title function_">find</span>(&#123;&#125;).<span class="title function_">then</span>(<span class="function"><span class="params">result</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(result)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p><code>.find()</code>、<code>.save()</code> 这些方法，都是挂在这个 <strong>模型对象（也就是变量 <code>Person</code>）</strong> 上的，而不是数据库里的“表名”上。</p><blockquote><p><strong>你在代码里能直接用的，全都是「变量名」</strong></p></blockquote><hr><h2 id="2-模型名：’Person’-——-给-Mongoose-用的内部名字"><a href="#2-模型名：’Person’-——-给-Mongoose-用的内部名字" class="headerlink" title="2. 模型名：’Person’ —— 给 Mongoose 用的内部名字"></a>2. 模型名：’Person’ —— 给 Mongoose 用的内部名字</h2><p>再看这一段：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mongoose.<span class="title function_">model</span>(<span class="string">&#x27;Person&#x27;</span>, phonebookSchema)</span><br><span class="line"><span class="comment">//              ^^^^^^^  这是一个字符串，叫模型名（Model name）</span></span><br></pre></td></tr></table></figure><p><code>&#39;Person&#39;</code> 是一个<strong>字符串</strong>，专门用来告诉 Mongoose：</p><blockquote><p>“帮我注册一个叫 Person 的模型，它的结构是 phonebookSchema。”</p></blockquote><p>这个模型名有几个作用：</p><ol><li><p>在别的地方取回这个模型</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 在另一个文件里</span></span><br><span class="line"><span class="keyword">const</span> <span class="title class_">Person</span> = mongoose.<span class="title function_">model</span>(<span class="string">&#x27;Person&#x27;</span>) <span class="comment">// 不传 schema = 取模型</span></span><br></pre></td></tr></table></figure></li><li><p>Mongoose 会根据 <strong>模型名</strong> 推导出 <strong>集合名</strong></p><ul><li><code>&#39;Person&#39;</code> → <code>people</code></li><li><code>&#39;User&#39;</code> → <code>users</code></li><li><code>&#39;Phonebook&#39;</code> → <code>phonebooks</code></li></ul></li><li><p><code>Person.modelName</code> 就是这个字符串：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title class_">Person</span>.<span class="property">modelName</span>) <span class="comment">// &#x27;Person&#x27;</span></span><br></pre></td></tr></table></figure></li></ol><blockquote><p><strong>模型名是 Mongoose 在内部识别&#x2F;登记模型用的名字，不是 JS 变量名，也不是 MongoDB 里的集合名。</strong></p></blockquote><hr><h2 id="3-集合名（“表名”）：MongoDB-里真正存数据的名字"><a href="#3-集合名（“表名”）：MongoDB-里真正存数据的名字" class="headerlink" title="3. 集合名（“表名”）：MongoDB 里真正存数据的名字"></a>3. 集合名（“表名”）：MongoDB 里真正存数据的名字</h2><p>在 MongoDB 里叫 <strong>Collection（集合）</strong>,但是我习惯把他叫成表名。</p><p>当你写：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title class_">Person</span> = mongoose.<span class="title function_">model</span>(<span class="string">&#x27;Person&#x27;</span>, phonebookSchema)</span><br></pre></td></tr></table></figure><p>Mongoose 会自动推导出一个集合名，一般规则是：</p><ul><li>全小写 + 复数</li><li>特殊单词会稍微“智能复数化”一下，比如 <code>Person</code> → <code>people</code></li></ul><p>所以常见情况：</p><ul><li>模型名 <code>&#39;Person&#39;</code> → 集合名 <code>people</code></li><li>模型名 <code>&#39;Phonebook&#39;</code> → 集合名 <code>phonebooks</code></li><li>模型名 <code>&#39;User&#39;</code> → 集合名 <code>users</code></li><li>模型名 <code>&#39;City&#39;</code> → 集合名 <code>cities</code></li></ul><p>你可以在 MongoDB shell &#x2F; mongosh 里看到它：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">use Phonebook</span><br><span class="line">show collections</span><br><span class="line"><span class="comment"># 可能输出：</span></span><br><span class="line"><span class="comment"># people</span></span><br><span class="line"><span class="comment"># phonebooks</span></span><br></pre></td></tr></table></figure><blockquote><p><strong>集合名是数据库那边实际存在的名字，存的就是一条条文档（document）。</strong></p></blockquote><hr><h2 id="4-为啥-find-要用变量名，不是“表名”？"><a href="#4-为啥-find-要用变量名，不是“表名”？" class="headerlink" title="4. 为啥 .find() 要用变量名，不是“表名”？"></a>4. 为啥 <code>.find()</code> 要用变量名，不是“表名”？</h2><p>这是我一开始踩坑的地方。</p><p>错误写法：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title class_">Persons</span> = mongoose.<span class="title function_">model</span>(<span class="string">&#x27;Phonebook&#x27;</span>, <span class="title class_">PhonebookSchema</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// ❌ 下面这样会报错：persons is not defined</span></span><br><span class="line">phonebooks..<span class="title function_">find</span>(&#123;&#125;).<span class="title function_">then</span>(<span class="function"><span class="params">result</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(result)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>问题在于：</p><p>哪怕你知道数据库里的集合名是 <code>phonebooks</code>，你也不能直接这样调用</p><p>因为在 JS 里压根没有这个变量。</p><p><code>.find()</code> 的调用对象是 <strong>Mongoose 模型对象</strong>，也就是那个你自己声明的变量：</p><p>✔ 正确写法：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title class_">Persons</span> = mongoose.<span class="title function_">model</span>(<span class="string">&#x27;Phonebook&#x27;</span>, <span class="title class_">PhonebookSchema</span>)</span><br><span class="line"></span><br><span class="line"><span class="title class_">Persons</span>.<span class="title function_">find</span>(&#123;&#125;).<span class="title function_">then</span>(<span class="function"><span class="params">result</span> =&gt;</span> &#123;</span><br><span class="line">  result.<span class="title function_">forEach</span>(<span class="function"><span class="params">person</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(person)</span><br><span class="line">  &#125;)</span><br><span class="line">  mongoose.<span class="property">connection</span>.<span class="title function_">close</span>()</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><blockquote><p><strong>你始终在操作「模型（变量）」：<code>Person</code> &#x2F; <code>Persons</code>，而不是直接操作集合名 <code>people</code> &#x2F; <code>phonebooks</code>。</strong></p></blockquote><hr><h2 id="6-三者关系总结"><a href="#6-三者关系总结" class="headerlink" title="6. 三者关系总结"></a>6. 三者关系总结</h2><p>再回到这行代码：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title class_">Person</span> = mongoose.<span class="title function_">model</span>(<span class="string">&#x27;Person&#x27;</span>, phonebookSchema)</span><br></pre></td></tr></table></figure><p>可以这样理解：</p><ul><li><p><code>Person</code>（左边）：<br>👉 JavaScript 变量名，代表一个 <strong>模型对象</strong>，用来写 <code>Person.find()</code> &#x2F; <code>new Person()</code>。</p></li><li><p><code>&#39;Person&#39;</code>（右边字符串）：<br>👉 <strong>模型名（Model name）</strong>，Mongoose 用它来管理模型、推导集合名。</p></li><li><p><code>people</code>（推导出来的集合名）：<br>👉 MongoDB 里的 <strong>集合名（Collection &#x2F; “表名”）</strong>，真正存数据的地方。</p></li></ul><p>用一张“小翻译表”概括：</p><table><thead><tr><th>概念</th><th>示例</th><th>属于哪里</th><th>用来干嘛</th></tr></thead><tbody><tr><td>变量名</td><td><code>Person</code></td><td>JavaScript 代码</td><td>调用 <code>.find()</code> &#x2F; <code>new</code> &#x2F; <code>.save()</code></td></tr><tr><td>模型名</td><td><code>&#39;Person&#39;</code></td><td>Mongoose 内部</td><td>注册 &#x2F; 取回模型，推导集合名</td></tr><tr><td>集合名</td><td><code>people</code></td><td>MongoDB 数据库</td><td>真正存文档的地方</td></tr></tbody></table><hr><h2 id="7-一点踩坑小结"><a href="#7-一点踩坑小结" class="headerlink" title="7. 一点踩坑小结"></a>7. 一点踩坑小结</h2><p>我在写 phonebook 脚本时，踩过的典型坑：</p><ol><li><p><strong>变量名大小写写错</strong></p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title class_">Persons</span> = mongoose.<span class="title function_">model</span>(<span class="string">&#x27;Phonebook&#x27;</span>, <span class="title class_">PhonebookSchema</span>)</span><br><span class="line"></span><br><span class="line">persons.<span class="title function_">find</span>(&#123;&#125;)   <span class="comment">// ❌ ReferenceError: persons is not defined</span></span><br></pre></td></tr></table></figure></li><li><p>把<strong>模型名&#x2F;变量名&#x2F;“表名”混在一起理解</strong>，以为：</p><blockquote><p>“集合叫 phonebooks，那我就写 phonebooks.find() 吧？”</p></blockquote><p>其实 <code>.find()</code> 的对象是「模型变量」，不是“表名”。</p></li><li><p>在一个脚本里又 <code>.save()</code> 又 <code>.find()</code>，并且在不同的 <code>then</code> 里乱关 <code>mongoose.connection.close()</code>，导致还没查完连接就被关掉——这个属于额外 bonus 坑 🤣。</p></li></ol><hr><h2 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h2><p>如果你也在学 Mongoose，建议在脑子里时刻分清这三个层次：</p><ul><li><strong>我现在在写的是 JS 代码（变量名）</strong></li><li><strong>Mongoose 里登记的是模型名</strong></li><li><strong>数据库真正存在的是集合名</strong></li></ul><p>搞清楚之后，<code>Person.find()</code> 这种写法就不会再迷糊了，而看到 <code>people</code> &#x2F; <code>phonebooks</code> 这样的集合名时，也知道是 Mongoose 帮你生成的“数据库那一侧”的名字。</p><p>希望这篇小记能帮你少掉几个 <code>xxx is not defined</code> 😄</p>]]></content>
      
      
      <categories>
          
          <category> fullstackopen </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Node.js </tag>
            
            <tag> mongos </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>fullstackopen学习</title>
      <link href="/2025/12/04/fullstackopen%E5%AD%A6%E4%B9%A0/"/>
      <url>/2025/12/04/fullstackopen%E5%AD%A6%E4%B9%A0/</url>
      
        <content type="html"><![CDATA[<p>最近开始系统性地学习赫尔辛基大学的 <strong>Full Stack Open</strong> 课程。<br>这篇文章主要记录我在学习 Part 1 到 Part 3 过程中的一些心得，以及在代码上踩过的坑。</p><h2 id="🛠️-技术栈概览"><a href="#🛠️-技术栈概览" class="headerlink" title="🛠️ 技术栈概览"></a>🛠️ 技术栈概览</h2><p>Full Stack Open 课程主要涵盖了现代 Web 开发的核心技术栈：</p><ul><li><strong>Frontend</strong>: React.js (Hooks, State Management)</li><li><strong>Backend</strong>: Node.js &amp; Express</li><li><strong>Deployment</strong>: Fly.io</li></ul><span id="more"></span><h2 id="💡-学习心得"><a href="#💡-学习心得" class="headerlink" title="💡 学习心得"></a>💡 学习心得</h2><h3 id="1-React-的特性"><a href="#1-React-的特性" class="headerlink" title="1. React 的特性"></a>1. React 的特性</h3><p>React 的 <strong>声明式</strong> 写法是一大特色。<br>特别是 <code>useState</code> 和 <code>useEffect</code> 这两个 Hook：</p><ul><li><strong>State</strong>: 它是组件的“记忆”，状态改变引发视图更新。如果单纯进行数据的修改页面将不会刷新</li></ul><h3 id="2-后端与-RESTful-API"><a href="#2-后端与-RESTful-API" class="headerlink" title="2. 后端与 RESTful API"></a>2. 后端与 RESTful API</h3><p>进入 Part 3 后，开始用 <strong>Express</strong> 写后端。</p><p>最大的感触是理解了 <strong>REST API</strong> 的设计理念。通过 <code>GET</code>, <code>POST</code>, <code>DELETE</code> 等不同的 HTTP 动词来操作资源。</p><p>调试后端时，我也遇到过不少坑，比如：</p><ul><li><strong>CORS</strong> (跨域资源共享) 问题，导致前端连不上后端。</li><li>在部署的时候忘记更改URL</li><li>前后端分离之后的时候main.jsx读取数据的方式应当修改</li></ul><h3 id="3-关于部署-Deployment"><a href="#3-关于部署-Deployment" class="headerlink" title="3. 关于部署 (Deployment)"></a>3. 关于部署 (Deployment)</h3><p>把代码跑在本地 (<code>localhost:3000</code>) 和部署到公网完全是两码事。</p><p>我选择了课程推荐的 <strong>Fly.io</strong> 进行部署。虽然中间经历了 <code>flyctl</code> 路径找不到、鉴权失败等问题，但最终看到自己的应用在公网上还是跑起来了</p>]]></content>
      
      
      <categories>
          
          <category> fullstackopen </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Node.js </tag>
            
            <tag> Coding </tag>
            
            <tag> React </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
